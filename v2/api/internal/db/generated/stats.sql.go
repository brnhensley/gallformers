// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: stats.sql

package db

import (
	"context"
)

const countGallFamilies = `-- name: CountGallFamilies :one
SELECT COUNT(DISTINCT family.id) as count
FROM taxonomy family
INNER JOIN taxonomy genus ON genus.parent_id = family.id AND genus.type = 'genus'
INNER JOIN speciestaxonomy st ON st.taxonomy_id = genus.id
INNER JOIN species s ON s.id = st.species_id
WHERE family.type = 'family' AND s.taxoncode = 'gall'
`

// Count distinct families that have gall species
func (q *Queries) CountGallFamilies(ctx context.Context) (int64, error) {
	row := q.db.QueryRowContext(ctx, countGallFamilies)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const countGallGenera = `-- name: CountGallGenera :one
SELECT COUNT(DISTINCT genus.id) as count
FROM taxonomy genus
INNER JOIN speciestaxonomy st ON st.taxonomy_id = genus.id
INNER JOIN species s ON s.id = st.species_id
WHERE genus.type = 'genus' AND s.taxoncode = 'gall'
`

// Count distinct genera that have gall species
func (q *Queries) CountGallGenera(ctx context.Context) (int64, error) {
	row := q.db.QueryRowContext(ctx, countGallGenera)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const countHostFamilies = `-- name: CountHostFamilies :one
SELECT COUNT(DISTINCT family.id) as count
FROM taxonomy family
INNER JOIN taxonomy genus ON genus.parent_id = family.id AND genus.type = 'genus'
INNER JOIN speciestaxonomy st ON st.taxonomy_id = genus.id
INNER JOIN species s ON s.id = st.species_id
WHERE family.type = 'family' AND s.taxoncode = 'plant'
`

// Count distinct families that have host/plant species
func (q *Queries) CountHostFamilies(ctx context.Context) (int64, error) {
	row := q.db.QueryRowContext(ctx, countHostFamilies)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const countHostGenera = `-- name: CountHostGenera :one
SELECT COUNT(DISTINCT genus.id) as count
FROM taxonomy genus
INNER JOIN speciestaxonomy st ON st.taxonomy_id = genus.id
INNER JOIN species s ON s.id = st.species_id
WHERE genus.type = 'genus' AND s.taxoncode = 'plant'
`

// Count distinct genera that have host/plant species
func (q *Queries) CountHostGenera(ctx context.Context) (int64, error) {
	row := q.db.QueryRowContext(ctx, countHostGenera)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const countUndescribedGalls = `-- name: CountUndescribedGalls :one

SELECT COUNT(*) as count
FROM species s
INNER JOIN gallspecies gs ON gs.species_id = s.id
INNER JOIN gall g ON gs.gall_id = g.id
WHERE s.taxoncode = 'gall' AND g.undescribed = 1
`

// =============================================================================
// Site Statistics Queries
// =============================================================================
// Count galls that are marked as undescribed
func (q *Queries) CountUndescribedGalls(ctx context.Context) (int64, error) {
	row := q.db.QueryRowContext(ctx, countUndescribedGalls)
	var count int64
	err := row.Scan(&count)
	return count, err
}
